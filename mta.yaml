_schema-version: "3.2"
ID: project1git
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: project1git-destination-content
  type: com.sap.application.content
  requires:
  - name: project1git-destination-service
    parameters:
      content-target: true
  - name: project1git-repo-host
    parameters:
      service-key:
        name: project1git-repo-host-key
  - name: project1git-uaa
    parameters:
      service-key:
        name: project1git-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: project1git_html_repo_host
          ServiceInstanceName: project1git-html5-srv
          ServiceKeyName: project1git-repo-host-key
          sap.cloud.service: project1git
        - Authentication: OAuth2UserTokenExchange
          Name: project1git_uaa
          ServiceInstanceName: project1git-xsuaa-srv
          ServiceKeyName: project1git-uaa-key
          sap.cloud.service: project1git
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: project1git-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: project1git-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project1git.zip
      name: project1git
      target-path: resources/
- name: project1git
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: project1git-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: project1git-destination-service
    service-plan: lite
- name: project1git-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: project1git-xsuaa-srv
    service-plan: application
- name: project1git-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: project1git-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
